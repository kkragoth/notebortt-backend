apiVersion: apps/v1
kind: Deployment
metadata:
  name: realtime
  namespace: prod
spec:
  replicas: 2
  selector:
    matchLabels:
      app: realtime
  template:
    metadata:
      labels:
        app: realtime
    spec:
      terminationGracePeriodSeconds: 60
      containers:
        - name: realtime
          image: ghcr.io/your-org/note-canva-backend:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3000
          env:
            - name: APP_ROLE
              value: realtime
          envFrom:
            - configMapRef:
                name: backend-config
            - secretRef:
                name: backend-secrets
          readinessProbe:
            httpGet:
              path: /health
              port: 3000
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 3000
            initialDelaySeconds: 30
            periodSeconds: 20
          resources:
            requests:
              cpu: "250m"
              memory: "256Mi"
            limits:
              cpu: "1000m"
              memory: "1Gi"
---
apiVersion: v1
kind: Service
metadata:
  name: realtime
  namespace: prod
spec:
  selector:
    app: realtime
  ports:
    - name: http
      port: 80
      targetPort: 3000
  type: ClusterIP
